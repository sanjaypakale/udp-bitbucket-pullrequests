// Main components
export { Root } from './Root';
export { UserProfilePopup } from './UserProfilePopup';

// Types (for external use if needed)
export type { 
  UserProfile, 
  UserProfilePopupProps, 
  AvatarConfig 
} from './UserProfilePopup.types';

// Utils (for external use if needed)
export { 
  getUserInitials, 
  getAvatarColor, 
  DEFAULT_USER_PROFILE 
} from './UserProfilePopup.utils';
